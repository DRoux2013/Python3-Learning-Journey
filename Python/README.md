# Python 3 Learning Journey: IT Automation (D522)

A structured log tracking my progression, code syntax mastery, and automation scripts for the D522 Python for IT Automation course.

## 🛠️ Workspace & Environment
* **OS Distribution:** Linux COSMIC Distro (Pop!_OS)
* **Local IDE:** Visual Studio Code (VS Code) + Official Python Extension
* **Execution Layout:** Command-Line Interface (CLI) & Python REPL Scratchpad

## 📈 Accountability & Metrics
* **Core Syllabus:** ZyBooks (Course Planning Tool Complete)
* **Daily Supplementary Target:** Coddy Python Fundamentals Journey
* **XP Milestone:** 195 XP / day (Monday–Friday)

---

## 🗄️ Progress & Competency Log

### 🔹 Week 1: Foundational Syntax, Blocks, and Data Shapes
* **Competency 4124.1.1 (Applies Python Principles and Syntax):**
  * Conquered the core data types (`str`, `int`, `bool`).
  * Mastered Python sequence indexing rules (**Zero-Indexed** for forward tracking, **Negative-Indexing `[-1]`** for log auditing / grabbing the last element).
  * Built, executed, and debugged a custom dictionary (`router_config.py`) mapping network device profiles using key-value structures.
  * Overcame first real-world `SyntaxError` (matching brackets vs. curly braces pairing rules).

* **Competency 4124.1.2 (Creates Python Scripts Using Control Structures):**
  * Explored Python **Whitespace & Indentation Architecture** (PEP 8 standard: 4 spaces per block level).
  * Investigated text parsing mechanics (`.split('\n')` for row isolation and `.split()` for columnar fields) to translate raw command strings into functional query targets.
  * Deep-dived into structural block composition vs. variable scope leakage rules (verifying variables initialized inside basic conditional blocks remain accessible outside them).
